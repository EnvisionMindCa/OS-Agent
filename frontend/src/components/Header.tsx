import React from 'react';
import '../styles/Header.css'; // We'll create this file

const Header: React.FC = () => {
  return (
    <header className="app-header">
      <h1>OS Agent Interface</h1>
    </header>
  );
};

export default Header;
